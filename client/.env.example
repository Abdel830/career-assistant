# Base URL for Backend API in production (e.g. Render backend service)
# In local development, leave empty to use Vite dev server proxy (/api)
VITE_API_BASE_URL=https://your-backend-api.onrender.com/api
